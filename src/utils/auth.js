const jwt = require('jsonwebtoken');

// const generateToken = (data) => return 

// Implementar com jwt
const encryptPassword = (password) => {
    return password
}

module.exports = {
    encryptPassword
}